Set `Zone-Level FEFO` as the removal strategy of a location, or of a product
category.

As for Zone-Level FIFO, the zone entry date is only maintained if a module
defining the zones is installed, for instance
`stock_removal_zone_fifo_location_zone`.
